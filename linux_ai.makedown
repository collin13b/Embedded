# linux常用命令
pwd：当前目录
ls：查看目录下文件（ls -l显示详细信息,ls -a显示隐藏文件）
mkdir:创建
rm:删除 rm -rf *,rm -r dir/删除目录及内容
htop:查看 cpu 和内存占用
ps -ef：所以进程
kill -9 进程 id ：强制删除
uname -a ： 内核信息
cat 文件名:查看文件
echo:输出文本
find /home -name "*.txt" (在指定目录下按名查找文件)
mv file.txt /home/ (移动文件)
cp file1 file2
touch newfile.txt (创建空文件或更新文件时间戳)
 grep ：全局正则搜索
 head -n 需要的行数 文件名：查看文件头几行
 tail -f   文件名       ：查看文件末尾


# 安装opencv
# 安装编译工具
sudo apt install build-essential cmake git pkg-config

# 安装图像/视频 I/O 库
sudo apt install libjpeg-dev libpng-dev libtiff-dev
sudo apt install libavcodec-dev libavformat-dev libswscale-dev libv4l-dev
sudo apt install libxvidcore-dev libx264-dev

# 安装 GUI 和优化库
sudo apt install libgtk-3-dev
sudo apt install libatlas-base-dev gfortran
# 如果需要额外模块（可选，但推荐）
git clone https://github.com/opencv/opencv_contrib.git

# CV
    cv::Mat img;
        img = cv::imread("/home/cat/linux_Ai/img2.png",cv::IMREAD_COLOR);
        cv::imwrite("/home/cat/linux_Ai/write.png",img);
捕获视频 cv::VideoCapture cap(video_path);
        cap.read() 读取一帧照片
        cap.isOpened（）判断是打开路径
        (cap.get(cv::CAP_PROP_FPS));视频帧数
        (cap.get(cv::CAP_PROP_FRAME_HEIGHT));高
        (cap.get(cv::CAP_PROP_FRAME_WIDTH));款
        size（宽，高），视频大小
       cv::VideoWriter writer(video_output_path,
       cv::VideoWriter::fourcc('H','2','6','4'),video_fps,cv::Size(width,height));
       写出视频，视频路径，编码格式，fps，size，必须要释放writer.relase;
# Safequeue 安全队列
## mutable 保证在const的情况下可以加锁
## mutex
使用 mutex 互斥锁去加载视频帧，确保数据不丢失
## atomic 原子变量，保护共享资源，不会同时被改变防止竞争
load() - 原子地读取值
store(value) - 原子地存储值
exchange(value) - 原子地交换值
compare_exchange_weak() / compare_exchange_strong() - 比较并交换
## 条件变量
wait 和 wait_for
wait 为无限等待
wait_for 有超时时间
    q_cond.wait_for(lock, chrono::milliseconds(100) ,
            [this]{return q.size() < max_size; });//等待条件变量，直到队列不满
## C++ Lambda使用方法
[capture](parameters) mutable → return_type {body}
[]	不捕获任何外部变量。
[x]	按值捕获变量 x。Lambda 内部拥有 x 的副本。
[&x]	按引用捕获变量 x。修改 Lambda 内部的 x 会影响外部。
[=]	隐式按值捕获所有外部变量。
[&]	隐式按引用捕获所有外部变量。
[this]	捕获当前类实例的指针，用于在成员函数中使用 Lambda。
# 线程池
